# whoami 前端设计上下文

## Design Context

### Users

- 站长本人（开发新手）：通过管理后台维护个人主页内容。
- 访客：浏览个人主页（技术栈、作品、经历、简历下载），多为程序员同行。

### Brand Personality

- 三词：极客、复古未来、硬核玩心。
- 目标情绪：访客打开的第一眼是"这是个懂行的开发者"，而非"这是个模板站"。
- 反参照：不要 SaaS 蓝紫渐变、玻璃拟态、通用后台模板脸。

### Aesthetic Direction

- 风格：终端黑客风 + 像素风 + 赛博科技风三合一。
- 主题：纯暗色（`--bg: #04070b`），主色磷光绿 `#00ff9c`，辅色赛博青 `#2bd9ff` / 品红 `#ff2e88`。
- 字体策略：像素字体 Press Start 2P 只用于 ASCII 标识（logo、模块编号、HUD 标签）；VT323 用于终端读数行（纯 ASCII）；中文一律等宽栈（Cascadia Code → 系统回退），不引入中文像素字体。
- 质感层：全局 CRT 覆盖（扫描线 + 暗角 + 慢速滚动亮带，App.vue 实现），页面级点阵网格背景、HUD 四角括号取景框、辉光 text-shadow、hover 色差（品红/青错位）。
- 圆角一律为 0（锐利直角 = 像素身份）。
- 关键动效：打字机循环（首页）、logo 故障艺术 glitch、LED 脉冲、卡片/区块 rise-in 交错入场（35-40ms 步进）。

### Design Principles

1. 功能优先于装饰：登录、表单、管理操作不得被特效干扰；CRT 覆盖层 `pointer-events: none`。
2. 一个页面一个焦点：每页只设一个 hero 级视觉时刻（首页是 glitch logo，登录页是终端窗，管理首页是状态网格）。
3. `prefers-reduced-motion` 必须完整降级：glitch / 打字机 / 滚动亮带 / 抖动全部静止，静态版依然成立。
4. 像素字体只碰 ASCII：中英混排时中文走等宽栈，避免字体大小打架。
5. 赛博感来自细节而非堆料：色差、扫描线、状态灯这类小点缀优先于大面积渐变与粒子。
