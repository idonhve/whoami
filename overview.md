# Overview — 个人简历网站需求定稿

## 完成了什么

两轮 grill 访谈（9 + 8 问）将站主 11 条初始想法打磨为可执行需求，产出 PRD v1.1，并沉淀领域术语表与架构决策记录，为 to-spec 与多窗口并行开发做好准备。

## 关键决策记录

### 第一轮（PRD v1.0）

| 决策点 | 结论 |
| --- | --- |
| 定位 | 求职武器，受众优先级 HR/面试官 > 同行 > 客户 |
| 访客统计 | 埋点（IP/UA/时长/路径）+ 自愿留言登记，不强制登录 |
| 管理端 | 轻量全管后台（单人管理员 + JWT），管所有动态数据 |
| GitHub 数据 | 后端定时同步进 MySQL，前台不直连 GitHub API |
| 特效策略 | 全站重特效 + 三条安全带（每页一个 3D 锚点 / 移动端降级 / 感知等待 <2s） |
| 视觉人格 | 程序员终端风（开机动画 + 整站即操作系统概念） |
| 加分功能 | 一期：命令面板 + 访客地图 + 控制台彩蛋；二期：AI 聊天分身 |
| 工作经历 | 战果视觉墙（大数字翻牌 + 雷达图 + 技术标签云，文字 ≤30 字/卡） |
| 技术栈 | Vue3 + Vite + TS + Three.js + ECharts / Spring Boot 3 + MySQL 8 |

### 第二轮（PRD v1.1，2026-08-29）

| 决策点 | 结论 |
| --- | --- |
| 服务器地域 | 境内 + ICP 备案；**购买延后**至本地验收通过，按代码量选配防浪费（M4 本地验收 / M5 上线拆分） |
| 域名 | 延后购买，开发期用 `site_config` 域名占位配置 |
| 管理员初始账号 | SQL 初始化脚本插入（bcrypt 密文） |
| IP 归属地 | ip2region 离线库 + 省级聚合打点（ADR-0002） |
| 中国地图数据源 | DataV.GeoAtlas 标准版图（版图完整性合规），版本锁定入库 |
| 核心表清单 | 12 张（补 `site_config`、`admin_op_log`、`track_event`） |
| spec 组织 | 按模块切分 `docs/spec/01~12`，与 F1~F12 对应 |
| 开发方式 | 多会话窗口并行，GitHub Issue（`idonhve/whoami`）协调，见 `docs/parallel-plan.md` |

## 文档索引

| 文档 | 用途 |
| --- | --- |
| `PRD.md` | 产品需求与验收标准（v1.1，含已决/代决/待办） |
| `CONTEXT.md` | 领域术语表（ glossary） |
| `docs/adr/` | 架构决策记录（0001 GitHub 后端同步 / 0002 ip2region 离线库） |
| `docs/content-checklist.md` | 站主素材准备清单（独立窗口收集用） |
| `docs/parallel-plan.md` | 多会话窗口并行开发流程与复制即用的开场白 |

## 后续事项

1. 窗口 A：to-spec → `docs/spec/01~12` + 创建 Issue（等它产出后再开工）
2. 窗口 B：素材收集（可与 A 并行，站主在场）
3. 窗口 C：M1 地基（串行，唯一窗口）
4. 窗口 D+：模块并行开发（2~4 窗口）
5. 上线（购服务器/域名/备案）延后至本地验收通过
