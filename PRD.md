# 个人简历网站（Personal Portfolio）产品需求文档

| 项目名称 | 个人简历网站                                   |
| ---- | ---------------------------------------- |
| 版本   | v1.1                                     |
| 状态   | 需求已确认（两轮 grill 访谈定稿）                     |
| 日期   | 2026-08-29                               |
| 产品形态 | 前后端分离 Web 应用（Vue3 + Spring Boot + MySQL） |

***

## 1. 产品定位

**一句话定位：一款"全站终端风、特效拉满"的求职武器型个人简历网站，让面试官在 30 秒内记住站主。**

* 第一目标：**求职**——让 HR/面试官快速建立印象、产生约面意愿

* 受众优先级：HR/面试官 > 技术同行 > 潜在客户

* 差异化主张：不是"又一个小清新博客"，而是"整站即操作系统"的程序员终端风体验

* 核心成功指标：面试官打开网站 → 30 秒内看懂"这个人是谁、最强的是什么" → 下载简历/跳转 GitHub → 发出面试邀请

### 1.1 设计原则（约束所有功能）

1. **视觉优先于文字**：能用数字动效、图表、图标表达的，不写段落文字；单卡片文字量 ≤ 30 字
2. **全站重特效 + 安全带**：每页一个 3D/重动效锚点；移动端与低配设备自动降级；首屏感知等待 < 2 秒
3. **数据全动态**：所有展示内容由管理后台维护，改内容不改代码、不发版

***

## 2. 技术架构

### 2.1 技术选型

| 层      | 选型                                       | 说明                                                       |
| ------ | ---------------------------------------- | -------------------------------------------------------- |
| 前端     | Vue 3 + Vite + TypeScript + Pinia        | 组合式 API，构建快                                              |
| 可视化/特效 | Three.js + GSAP + ECharts                | 3D 锚点场景、滚动动效、技术栈图表                                       |
| UI 基础  | Naive UI（暗色主题定制）                         | 与终端风视觉统一                                                 |
| 后端     | Java 17 + Spring Boot 3.x + MyBatis-Plus | REST API，JWT 鉴权（仅管理员）                                    |
| 数据库    | MySQL 8.0                                | 全量业务数据                                                   |
| 部署     | Docker Compose（Nginx + Java + MySQL 三容器） | 开发期本地一键起；生产为境内 2C2G 轻量云（含 ICP 备案），本地验收通过后按实际代码量购入，避免资源浪费 |
| 证书/域名  | Let's Encrypt 免费 HTTPS + 自有域名            | 年成本约 600-1200 元（购买延后，见 M5）                               |

### 2.2 核心数据表（概要）

共 12 张（术语定义见 `CONTEXT.md`）：

`tech_stack`（技术栈）、`project`（作品/GitHub 仓库）、`experience`（工作经历/战果）、`certificate`（证书/奖状）、`resume_file`（简历 PDF 版本）、`guest_message`（访客留言）、`visit_log`（访问日志）、`track_event`（行为事件：简历下载/命令面板使用/彩蛋触发/GitHub 外跳/留言提交）、`admin_user`（管理员）、`admin_op_log`（后台操作日志）、`site_config`（站点配置键值对）、`sync_task_log`（GitHub 同步日志）。

### 2.3 GitHub 数据链路

后端定时任务（每日 03:00 + 后台手动触发）拉取 GitHub REST API → 写入 `project` 表 → 管理后台支持筛选/置顶/隐藏/编辑中文描述。**前台页面永不直连 GitHub API。**（决策记录：ADR-0001）

鉴权使用站主 GitHub 个人访问令牌（只读公开仓库权限），经环境变量注入，不入库、不下发前端。

***

## 3. 功能需求与验收标准

> 验收标准定义"做到什么程度才算完成"。每条功能含：需求描述 + 验收标准（可勾选的 DoD）。

### F1. 欢迎页（终端开机动画）

**需求**：全站入口为一个"终端开机"欢迎页，随后进入主页。整站视觉人格为程序员终端风（黑底 + 绿色/青色荧光 + 等宽字体），导航呈现为命令，页面切换像切窗口。

**验收标准**：

* [ ] 打开网站先看到全屏终端动画：逐行敲出 `> initializing <域名> ...`、`> loading portfolio ...` 等至少 5 行伪日志，带光标闪烁与打字音效（可静音）

* [ ] 日志打印完成后粒子过渡动画进入主页，总时长 ≤ 2.5 秒，且支持点击跳过

* [ ] 动画期间显示加载进度，无白屏

* [ ] 二次访问（同浏览器 24h 内）跳过开机动画直接进主页（localStorage 标记）

* [ ] 桌面端与移动端均正常播放（移动端为轻量版动画）

* [ ] 开机日志中的域名文案读取自 `site_config` 配置；域名未购买时显示占位符（本地为 `localhost`），改配置不改代码

### F2. 技术栈展示模块

**需求**：主页/关于页模块，用饼图（技术栈分布占比）+ 条状图（熟练度）可视化展示技术栈，数据来自后台维护。

**验收标准**：

* [ ] 提供饼图（按分类：前端/后端/数据库/工具等的数量或自定权重占比）与水平条状图（单项熟练度，条形有渐变发光填充动画）

* [ ] 每项技术含名称 + 图标（devicon 或同类图标库）+ 熟练度标签（如 精通/熟练/了解）

* [ ] 图表入场有滚动触发的渐入动画，hover 有 tooltip 明细

* [ ] 技术栈数据全部来自数据库，后台增删改后前台刷新即可见，无需发版

* [ ] 移动端图表自适应，可正常交互

### F3. GitHub 图标跳转

**需求**：页头/页脚展示 GitHub 图标，点击新标签页打开站主 GitHub 主页。

**验收标准**：

* [ ] GitHub 图标位于页头导航栏与页脚，带 hover 动效（如呼吸光晕）

* [ ] 点击以 `target="_blank"` 打开 GitHub 主页，URL 来自后台配置（`site_config`），改配置不改代码

* [ ] 图标在暗色背景下清晰可见，通过可访问性对比度检查

### F4. GitHub 作品展示模块

**需求**：展示站主 GitHub 仓库作品，数据由后端定时同步，支持置顶/精选/中文描述。

**验收标准**：

* [ ] 作品卡片展示：仓库中文名描述、语言色点、star/fork 数、最近更新时间、置顶标记

* [ ] 卡片点击跳转 GitHub 仓库页（新标签）

* [ ] 后台可置顶（最多 3 个）、隐藏、编辑中文描述；首页"精选作品"区只显示置顶项，作品页显示全部未隐藏项

* [ ] 定时任务每日自动同步一次；后台有"立即同步"按钮；同步失败在后台可见日志与原因

* [ ] GitHub 服务不可用时，前台展示最近一次同步的缓存数据，不报错、不空白

* [ ] 卡片入场有滚动渐入 + hover 3D 倾斜效果

### F5. 访客统计（埋点 + 自愿登记）

**需求**：访客免登录自由浏览；系统记录访问 IP（解析归属地）、User-Agent、访问时间、停留时长、浏览路径、来源页；另提供自愿留名的访客留言板。

**验收标准**：

* [ ] 前台零登录门槛：不弹窗、不强制登记即可看完全部内容

* [ ] 前端埋点上报：页面 PV、进入/离开时间（计算停留时长）、来源 referrer；后台可见聚合报表（按日 PV/UV 曲线、TOP 页面、来源分布）

* [ ] 访客地图：后台与前台"关于本站"区域渲染中国地图打点（IP 归属地），按**省级聚合**，点上有呼吸动画，hover 显示"该省访问 N 次"及城市 TOP 列表

* [ ] IP 归属地解析使用离线库 ip2region（境内精确到城市级，零外部 API 依赖；决策记录：ADR-0002）

* [ ] 中国地图 GeoJSON 采用阿里 DataV.GeoAtlas 标准版图（含台湾、南海诸岛九段线），版本锁定并存放于本仓库，不依赖 CDN 实时拉取

* [ ] 留言板：访客可自愿填写昵称（必填）+ 留言内容（必填）+ 邮箱（选填）；提交成功有终端风 toast 反馈（`> message sent ✓`）

* [ ] 留言支持管理员后台回复与删除；前台仅展示已通过审核（默认自动通过）的留言

* [ ] 统计数据保留 ≥ 12 个月；IP 存储做脱敏说明（个人网站合规提示）

* [ ] 埋点上报异步且不阻塞页面渲染（sendBeacon 或 img 打点）

### F6. 管理后台（轻量全管 CMS）

**需求**：仅站主一人可登录的后台，管理全部动态数据：简历 PDF、作品、技术栈、证书、工作经历、访问统计、留言、站点配置（含 GitHub 链接）。

**验收标准**：

* [ ] 后台入口为独立路由 `/admin`（前台不放显式入口）；登录用账号密码（bcrypt 存储）+ JWT，连续失败 5 次锁定 10 分钟

* [ ] 初始管理员由 SQL 初始化脚本插入（bcrypt 密文，无明文），部署文档说明密文生成与首次改密方式

* [ ] 管理员登录后，前台页面出现悬浮的"管理"按钮（仅管理员可见）

* [ ] 简历管理：上传 PDF（≤ 20MB，仅 pdf 类型），上传后自动替换旧文件并保留最近 3 个历史版本可回滚

* [ ] 作品管理：列表 + 筛选 + 置顶/隐藏/编辑中文描述 + 立即同步 GitHub

* [ ] 技术栈管理：增删改技术项（名称/图标/分类/熟练度/权重）

* [ ] 证书管理：上传图片（jpg/png/webp，单张 ≤ 5MB），填名称与获取时间，支持排序

* [ ] 工作经历管理：增删改经历卡（公司/职位/时间/核心数字战果/技术标签/雷达图维度数据）

* [ ] 统计看板：PV/UV 曲线、访客地图、TOP 页面、留言列表（回复/删除）

* [ ] 所有后台操作有操作日志（`admin_op_log`），可追溯

* [ ] 未登录访问任何 `/admin/api/*` 接口返回 401，前端路由守卫跳转登录页

### F7. 简历下载（前台）

**需求**：前台提供简历下载按钮，下载当前后台维护的最新 PDF。

**验收标准**：

* [ ] 下载按钮位于首页显眼位置（首屏可达，不用滚动超过一屏）与"关于我"区域，按钮为终端风命令样式（如 `> download resume.pdf`），带 hover 光效

* [ ] 点击即下载最新版本 PDF，文件名含姓名与更新日期（如 `张三_简历_2026-08.pdf`）

* [ ] 下载行为本身计入埋点事件（后台可见简历下载次数——求职网站的核心转化指标）

* [ ] 后台尚未上传简历时，按钮隐藏且不报错

### F8. 照片墙 / 奖状栏模块

**需求**：展示证书、奖状照片的视觉墙，支持灯箱查看大图。

**验收标准**：

* [ ] 瀑布流或错落网格布局，图片懒加载；入场有逐个翻转/渐入动画

* [ ] 点击任意图片打开灯箱（Lightbox），支持左右切换、ESC/点击遮罩关闭

* [ ] hover 显示证书名称与获取时间；后台可排序

* [ ] 图片经压缩处理（后台生成缩略图，前台先加载缩略图再按需加载原图），首屏该模块图片总体积 ≤ 1MB

* [ ] 数据来自 `certificate` 表，后台增删改后前台即时生效

### F9. 工作经历模块（战果视觉墙）

**需求**：区别于传统简历的文字罗列——以"滚动点亮时间轴 + 视觉化战果卡片"呈现：大数字翻牌动画、雷达图、图标与技术标签云，文字压缩到最少。

**验收标准**：

* [ ] 滚动驱动：左侧发光主线路随滚动逐段点亮，当前经历卡片高亮

* [ ] 每段经历默认态为纯视觉卡片：① 核心数字战果用翻牌/滚动递增动画（如 `300%`、`50w+`）；② 雷达图展示该阶段能力维度（后台配置维度与数值）；③ 技术标签以图标云呈现

* [ ] 单卡片默认态文字 ≤ 30 字（公司+职位+时间）；点击/hover 展开才显示补充细节（展开内容也以要点列表为主，不写段落）

* [ ] 移动端时间轴切换为单列卡片 + 滚动渐入，动画保留但简化

* [ ] 数据来自 `experience` 表，后台可维护；雷达图维度可自定义（至少支持 3-8 个维度）

### F10. 命令面板（Ctrl+K）

**需求**：全站可唤出的命令行导航，与终端风视觉统一。

**验收标准**：

* [ ] `Ctrl+K`（Mac `Cmd+K`）或点击页头 `>_` 图标唤起命令面板

* [ ] 支持命令：`home / works / tech / experience / awards / resume / message / github / theme` 等，含模糊匹配与上下键选择、回车执行

* [ ] 输入 `help` 显示全部命令列表；未知命令返回终端风提示 `command not found`

* [ ] 命令执行带终端风回显动画（`> navigating to works ...`）

* [ ] 面板有历史记录（localStorage，最近 5 条）

### F11. 控制台彩蛋

**需求**：面向打开 F12 的技术型访客埋彩蛋，促进传播。

**验收标准**：

* [ ] 控制台输出 ASCII art 站主签名 + 招聘联系方式 + "你打开了控制台，说明你是对的人"类文案

* [ ] 键盘秘籍（如连按 `↑↑↓↓←→←→BA`）触发全站特效（如粒子雨/主题切换），有视觉反馈

* [ ] 彩蛋不影响任何正常功能与性能（零运行时开销，事件监听惰性绑定）

### F12.（二期）AI 聊天分身

**需求**：一期上线后迭代。右下角悬浮终端图标，点开可与"站主 AI 分身"对话，基于预置简历语料回答面试官常见问题（技术栈、项目细节、工作偏好等）。

**验收标准（二期细化，先占位）**：

* [ ] 对话 UI 为终端风聊天窗，流式输出

* [ ] 接入 LLM API（密钥服务端保管，不下发前端），语料来自数据库维护的 Q\&A 与个人资料

* [ ] 无法回答时引导用户留言板留言，不编造事实

* [ ] 对话轮次计入埋点统计

***

## 4. 非功能需求与验收标准

### 4.1 性能（安全带条款，硬性红线）

* [ ] 桌面端首屏可交互时间（TTI）≤ 2s（常规带宽），开机动画即加载遮罩，**全程无白屏**

* [ ] 每个页面有且仅有一个 3D/重动效锚点（PRD 层面控制总量）

* [ ] 移动端/低配设备自动降级：检测 `navigator.hardwareConcurrency ≤ 4` 或屏宽 < 768px 时，3D 场景替换为静态渐变背景 + 轻量 CSS 动效；提供后台开关强制预览满血版

* [ ] 3D 资源懒加载（进入视口前 500px 触发）；图片走缩略图策略；JS 包按路由分包，首包 gzip ≤ 500KB（不含 3D 场景）

* [ ] Lighthouse（桌面）Performance ≥ 75，Accessibility ≥ 90

### 4.2 安全

* [ ] 全站 HTTPS；接口参数校验（防注入/XSS）；留言内容过滤与频率限制（同 IP 每分钟 ≤ 3 条）

* [ ] JWT 过期时间 ≤ 2h，支持续签；管理员密码 bcrypt 存储；上传文件类型白名单校验

* [ ] 前台接口与管理接口域名路径隔离，管理接口全部 401 兜底

### 4.3 兼容与响应式

* [ ] Chrome / Edge / Safari / Firefox 最新两个大版本正常

* [ ] 微信内置浏览器（移动端）正常浏览（降级模式）

* [ ] 1920 / 1440 / 1280 / 平板 / 手机（≥ 375px）五档响应式无横向滚动条

***

## 5. 页面结构总览

```
/               欢迎动画 + 首页（Hero：姓名+一句话定位+打字机+3D锚点+入口命令）
/works          作品列表（GitHub 同步数据 + 精选置顶）
/tech           技术栈（饼图 + 条状图）
/experience     工作经历（战果视觉墙）
/awards         照片墙 / 奖状栏（灯箱）
/about          关于我（访客地图 + 留言板 + 简历下载）
/admin          管理后台（登录后：内容管理 + 统计看板）
全局            命令面板(Ctrl+K) + 控制台彩蛋 + GitHub图标 + 页脚
```

***

## 6. 里程碑分期

| 阶段                    | 范围                                                                          | 完成标志                               |
| --------------------- | --------------------------------------------------------------------------- | ---------------------------------- |
| **M1 地基**（第 1-2 周）    | 工程搭建：Vue3+Vite+TS / Spring Boot+MySQL / Docker Compose 本地跑通；数据库建表；管理后台登录与鉴权 | `docker compose up` 一键起全栈；管理员可登录后台 |
| **M2 核心内容**（第 3-4 周）  | 欢迎页开机动画、首页、技术栈图表、作品展示（含 GitHub 同步）、后台内容管理（技术栈/作品/经历/证书/简历）                  | 所有静态内容可后台维护；F1-F4、F7-F9 验收通过       |
| **M3 统计与亮点**（第 5-6 周） | 埋点 + 访客地图 + 留言板 + 命令面板 + 控制台彩蛋 + 移动端降级                                      | F5、F10、F11 验收通过；性能红线达标             |
| **M4 本地验收**（第 7 周）    | 本地生产模式验收：docker compose 完整跑通、性能红线与 Lighthouse 本地达标、全部 P0 验收项通过              | 本地"完美"：全功能可用、红线达标                  |
| **M5 上线（延后，按需）**      | 按最终代码量选购境内服务器与域名、ICP 备案（2-4 周，与选购并行）、HTTPS、生产数据初始化                          | 线上可访问；购入时机由站主决定，防止资源浪费             |
| **二期迭代**              | AI 聊天分身（F12）                                                                | 视上线后流量决定                           |

***

## 7. 决策记录与待办

### 7.1 已决（2026-08-29 第二轮访谈）

* [x] 服务器地域：**境内 + ICP 备案**；购买延后至本地验收通过（M5），按代码量选配防止浪费

* [x] 域名：**延后购买**，开发期用 `site_config` 域名占位配置

* [x] 管理员初始账号：**SQL 初始化脚本插入**（bcrypt 密文）

* [x] IP 归属地：**ip2region 离线库 + 省级聚合打点**（ADR-0002）

* [x] 中国地图数据源：**DataV.GeoAtlas 标准版图，版本锁定入库**（版图完整性合规）

* [x] 核心表清单：**12 张**（见 2.2，后续按需增补）

* [x] spec 组织方式：**按模块切分** `docs/spec/01~12`，与 F1\~F12 对应

### 7.2 代决项（站主可随时推翻）

* 留言邮件通知：一期**不做**（后台看板可见即可，省去 SMTP 依赖），二期可选

* 埋点事件类型初版：`page_view` / `resume_download` / `cmd_palette_use` / `easter_egg` / `github_outbound` / `message_submit`

* GitHub 同步时刻：每日 03:00 + 后台手动触发

### 7.3 待办

* [ ] 站主素材收集：在独立会话窗口按 `docs/content-checklist.md` 完成

* [x] to-spec：生成 `docs/spec/01~12` 并创建对应 GitHub Issue（流程见 `docs/parallel-plan.md`）——已完成：`docs/spec/00~12` 共 13 册 + Issue #1~#13（#1 为 M1 地基串行优先，均打 `ready-for-agent`）

* [ ] GitHub PAT 创建（只读公开仓库权限）——已列入内容准备清单

